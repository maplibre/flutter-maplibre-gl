diff --git a/bindings/dart/lib/src/runtime/runtime.dart b/bindings/dart/lib/src/runtime/runtime.dart
index 03e76011..079b492e 100644
--- a/bindings/dart/lib/src/runtime/runtime.dart
+++ b/bindings/dart/lib/src/runtime/runtime.dart
@@ -1601,6 +1601,29 @@ final class MapHandle {
     _clearCustomGeometryCallbacks();
   }
 
+  /// Sets the style transition options.
+  ///
+  /// Negative [durationMs]/[delayMs] leave the corresponding option unset
+  /// (style defaults); a positive value must be finite and at most
+  /// 9223372036854, the largest a nanosecond duration can hold.
+  /// [enablePlacementTransitions] controls whether symbol placement changes
+  /// cross-fade (style default true); disabling it makes symbol updates apply
+  /// instantly, e.g. while dragging a feature.
+  void setStyleTransitionOptions({
+    double durationMs = -1,
+    double delayMs = -1,
+    required bool enablePlacementTransitions,
+  }) {
+    _check(
+      _c.raw.mln_map_set_style_transition_options(
+        _pointer,
+        durationMs,
+        delayMs,
+        enablePlacementTransitions,
+      ),
+    );
+  }
+
   /// Copies the current style as a full style-spec JSON document.
   String getStyleJson() {
     return withNativeArena((arena) {
diff --git a/include/maplibre_native_c/map.h b/include/maplibre_native_c/map.h
index 58cee194..519bd78b 100644
--- a/include/maplibre_native_c/map.h
+++ b/include/maplibre_native_c/map.h
@@ -1227,6 +1227,32 @@ mln_map_set_style_url(mln_map* map, const char* url) MLN_NOEXCEPT;
 MLN_API mln_status
 mln_map_set_style_json(mln_map* map, const char* json) MLN_NOEXCEPT;
 
+/**
+ * Sets the style transition options.
+ *
+ * duration_ms and delay_ms apply to style property transitions; a negative
+ * value leaves the corresponding option unset (style defaults), and a positive
+ * one must be finite and representable as a nanosecond duration, which caps it
+ * at 9223372036854 (about 292 years).
+ * enable_placement_transitions controls whether symbol placement changes
+ * cross-fade (the style default is true); disabling it makes symbol updates
+ * apply instantly, e.g. while dragging a symbol feature.
+ *
+ * This is a map command.
+ *
+ * Returns:
+ * - MLN_STATUS_OK on success.
+ * - MLN_STATUS_INVALID_ARGUMENT when map is null or not live, or when
+ *   duration_ms or delay_ms is non-finite or above 9223372036854.
+ * - MLN_STATUS_WRONG_THREAD when called from a thread other than the map owner
+ *   thread.
+ * - MLN_STATUS_NATIVE_ERROR when an internal exception is converted to status.
+ */
+MLN_API mln_status mln_map_set_style_transition_options(
+  mln_map* map, double duration_ms, double delay_ms,
+  bool enable_placement_transitions
+) MLN_NOEXCEPT;
+
 /**
  * Copies the current style as a full style-spec JSON document into
  * caller-owned memory.
diff --git a/src/c_api/map.cpp b/src/c_api/map.cpp
index 206b4924..72348594 100644
--- a/src/c_api/map.cpp
+++ b/src/c_api/map.cpp
@@ -113,6 +113,17 @@ auto mln_map_set_style_json(mln_map* map, const char* json) noexcept
   });
 }
 
+auto mln_map_set_style_transition_options(
+  mln_map* map, double duration_ms, double delay_ms,
+  bool enable_placement_transitions
+) noexcept -> mln_status {
+  return mln::c_api::status_boundary([&]() -> mln_status {
+    return mln::core::map_set_style_transition_options(
+      map, duration_ms, delay_ms, enable_placement_transitions
+    );
+  });
+}
+
 auto mln_map_copy_style_json(
   mln_map* map, char* out_json, size_t json_capacity, size_t* out_json_size
 ) noexcept -> mln_status {
diff --git a/src/map/map.cpp b/src/map/map.cpp
index 71ec1cb5..6de3521d 100644
--- a/src/map/map.cpp
+++ b/src/map/map.cpp
@@ -194,6 +194,27 @@ auto validate_zoom_option(double zoom, const char* name) -> mln_status {
   return MLN_STATUS_OK;
 }
 
+// Transitions are converted to mbgl::Duration, which counts nanoseconds in an
+// int64. Past the value it can hold the conversion is undefined rather than
+// merely absurd, and since the style spec sets no upper bound of its own that
+// is the only limit worth imposing (it works out to about 292 years).
+constexpr double kMaxTransitionMs = static_cast<double>(
+  std::chrono::duration_cast<std::chrono::milliseconds>(mbgl::Duration::max())
+    .count()
+);
+
+auto validate_transition_option(double milliseconds, const char* name)
+  -> mln_status {
+  if (!std::isfinite(milliseconds) || milliseconds > kMaxTransitionMs) {
+    auto message = std::string{name} + " must be finite and at most " +
+                   std::to_string(static_cast<int64_t>(kMaxTransitionMs)) +
+                   ", or negative to leave it unset";
+    mln::core::set_thread_error(message.c_str());
+    return MLN_STATUS_INVALID_ARGUMENT;
+  }
+  return MLN_STATUS_OK;
+}
+
 auto validate_tile_source_option_header(
   const mln_style_tile_source_options& options
 ) -> mln_status {
@@ -3252,6 +3273,39 @@ auto map_set_style_url(mln_map* map, const char* url) -> mln_status {
   return MLN_STATUS_OK;
 }
 
+auto map_set_style_transition_options(
+  mln_map* map, double duration_ms, double delay_ms,
+  bool enable_placement_transitions
+) -> mln_status {
+  const auto status = validate_map(map);
+  if (status != MLN_STATUS_OK) {
+    return status;
+  }
+  const auto duration_status =
+    validate_transition_option(duration_ms, "duration_ms");
+  if (duration_status != MLN_STATUS_OK) {
+    return duration_status;
+  }
+  const auto delay_status = validate_transition_option(delay_ms, "delay_ms");
+  if (delay_status != MLN_STATUS_OK) {
+    return delay_status;
+  }
+  auto options = mbgl::style::TransitionOptions{};
+  if (duration_ms >= 0) {
+    options.duration = std::chrono::duration_cast<mbgl::Duration>(
+      std::chrono::duration<double, std::milli>(duration_ms)
+    );
+  }
+  if (delay_ms >= 0) {
+    options.delay = std::chrono::duration_cast<mbgl::Duration>(
+      std::chrono::duration<double, std::milli>(delay_ms)
+    );
+  }
+  options.enablePlacementTransitions = enable_placement_transitions;
+  map->map->getStyle().setTransitionOptions(options);
+  return MLN_STATUS_OK;
+}
+
 auto map_set_style_json(mln_map* map, const char* json) -> mln_status {
   const auto status = validate_map(map);
   if (status != MLN_STATUS_OK) {
diff --git a/src/map/map.hpp b/src/map/map.hpp
index cb3e4276..c07b3f9e 100644
--- a/src/map/map.hpp
+++ b/src/map/map.hpp
@@ -41,6 +41,10 @@ auto map_request_repaint(mln_map* map) -> mln_status;
 auto map_request_still_image(mln_map* map) -> mln_status;
 auto map_set_style_url(mln_map* map, const char* url) -> mln_status;
 auto map_set_style_json(mln_map* map, const char* json) -> mln_status;
+auto map_set_style_transition_options(
+  mln_map* map, double duration_ms, double delay_ms,
+  bool enable_placement_transitions
+) -> mln_status;
 auto map_copy_style_json(
   mln_map* map, char* out_json, size_t json_capacity, size_t* out_json_size
 ) -> mln_status;
